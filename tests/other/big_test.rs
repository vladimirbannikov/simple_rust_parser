fn change_value(arr: &mut [i32], i: i32) {
    arr[1] = 10;
}

fn plus_one(x: i32) -> i32 {
    x + 1
}

fn main() {
{let a = 1;{}{}{}}
    let mut arr: [i32] = [1, 2, 3, 4];
    let mut a = 2;
    let ys: [i32] = [ret_1()];

	while x<y {
		break;
		break;
		break;
		break;
	}
    let q = 
    {
    	let i = 0;
    	let t = [i, 1+1, 2+2];
    	true && true && true;
    };
    println!("{}", q);
    let i = 0;
    let t = [i, 1+1, 2+2];
    true && true && true;
    change_value(&mut ((a)));
    change_value(&mut [1, 2, 3, 4]);
    let x = &mut 5;
change_value(&mut (1+1));
change_value(&mut (a));
    
    let mut test_arr = [1, 3, 4];
    test_arr[1+1] = 0;
}
